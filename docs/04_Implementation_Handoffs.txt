Purpose

This document records durable handoff guidance so future chats can resume work on the shared power system without re-litigating core architecture.

Use this file together with:

- 00_Master_Implementation_Plan
- 01_Locked_Decisions_Log
- 02_Power_System_Spec
- 03_Current_Phase_And_Next_Steps
- 05_Phase_6_Cost_Integration_Blueprint
- Power implementation steps


Current Phase Status

Phase 1 is complete.

Phase 2 is complete.

Phase 3 is complete enough to close.

Phase 4 is complete enough to close as a design phase and has hardened the packet-resolution spine.

Phase 5 — Implementation-Facing Translation of the Hardened Power Model — is complete enough to close as a design phase.

Phase 7 is substantially complete in Summoning Circle.

Phase 8 is substantially complete as a minimal but real monster power builder pass.

Phase 9 is in late-stage builder-surface validation and is close enough to close for the current Summoning Circle surface, even though niche edge cases may still appear.

Phase 11 is well advanced through preview formatting and builder UX cleanup and is close enough to close for the current builder surface.

Phase 6 costing is no longer hypothetical. It is active implementation and has reached a major calibration milestone.

The current Phase 6 toolchain already exists:

- canonical power-cost resolver
- versioned Power Tuning
- versioned Outcome Normalization
- versioned Combat Tuning / Monster Baseline Tuning
- shared canary catalog
- shared scenario catalog
- Canary Harness
- Outcome Calibration Harness
- active tuning snapshot export workflow
- active/default tuning modes for the calibration harness
- aligned live/harness stripped baseline
- active snapshot calibration currently passing 29 fixtures with 0 warnings

The next work is now real-monster validation, fixture expansion from real failures, remaining translation gaps, and later the monster-side realization layer.

A small number of implementation-facing boundary questions still remain open.

Those questions must not be used as an excuse to reopen solved architecture.


Rule for Future Builder Work

All future builder work should assume:

- one shared power core
- Summoning Circle as the first implementation surface
- Character Builder later reusing the same foundation
- standard monsters do not use the full player Spark / Restriction layer
- live preview is mandatory
- simple powers must stay simple
- only Packet 1 is defended against
- Lifespan, Effect Timing, and Effect Duration are separate concepts
- Counter is an authored Power-Level Field with No / Yes
- Assist is universal by system rule and is not authored per power
- Responses are the player-facing resource and are not the authored power permission field
- Trigger remains structurally separate from Counter
- Commitment Modifier uses Standard / Channel / Charge
- Channel uses one first-pass behaviour only as an action-commitment modifier
- first-pass Channel support is limited to Immediate, Field, and Attached
- hostile delayed-entry Attached powers must explicitly distinguish gate on attach from gate on payload
- Immediate + Standard Packet 1 currently uses On Cast only in the builder surface
- if an Immediate Packet 1 resolves On Cast and creates no beyond-turn carrier, later Immediate packets remain On Cast only in first implementation
- secondary scaling from the Primary result class is derived in the current builder surface rather than manually guessed
- persistent states use a stack-cleanup model distinct from ongoing direct effects
- ongoing damage uses ongoing value plus duration rather than stacks
- physical ongoing-damage cleanup defaults to Fortitude Resist and mental ongoing-damage cleanup defaults to Bravery Resist
- packet-local recipient class may distinguish Primary Targets, Allies, and Self where a Secondary Packet needs a different target class from Packet 1
- Allies includes self by default unless an explicit authored restriction says otherwise
- packet-local recipient class must remain legal within the shared cast context
- ambiguous Cleanse targeting of Active Power or Channelled Power may use Cleanse Theme only where a unique cleanup Resist Attribute is not otherwise already settled
- Trigger powers may use Trigger Method to distinguish Arm and then target from Target and then arm while keeping Packet 1 on trigger
- Secondary timing choices must obey both chassis legality and Packet 1 carrier legality
- Immediate remains the only chassis that can honestly have no Lifespan requirement in the current builder surface; Field, Attached, Trigger, and Reserve require Turns or Passive and default to Turns
- self-origin sphere wording should distinguish static areas centered on the user's current space from self-hosted Attached areas attached to the user
- overlapping areas from the same packet at the same timing use one power roll, pre-defence multiplication by overlap count, and one defence roll
- canonical attribute naming is Guard and Synergy
- Dodge succeeds on match or exceed
- future chats must not reintroduce exceed-only dodge wording

Future builder work should also preserve the following implementation-facing direction:

- shared cast context should remain centered in Power-Level Fields
- structural state should be represented through the selected chassis
- packet outcomes should be represented through Effect Packets
- resolved gates, legality warnings, and preview wording should be treated as derived metadata rather than co-equal authored truth
- local origin and local targeting overrides should stay behind advanced controls


Rule for Future Phase 6 Work

All future Phase 6 work must preserve the current three-layer split:

Power Tuning
- canonical power truth

Combat Tuning / Monster Baseline Tuning
- non-power monster baseline contribution

Outcome Normalization
- downstream radar interpretation and display

Do not collapse these into one fuzzy tuning system.

Use this ownership rule:

- if the power itself is wrong, tune Power Tuning
- if stripped monsters are too weak or too strong before powers, tune Combat Tuning
- if the combined result displays wrongly on the radar, tune Outcome Normalization
- if the authored monster does not contain the behaviour its name implies, fix monster authoring
- if the fixture expectation no longer matches the active tuning truth, fix the fixture expectation
- if the issue is routing law, realization law, or a missing implementation seam, then it is a code / architecture task rather than a numeric tuning task

The current live implementation already uses:

- canonical power contribution from the Phase 6 resolver
- derived cooldown turns from BasePowerValue using level-adjusted load/capacity brackets
- effective per-power availability/cooldown adjustment before final monster outcome
- non-power contribution from stripped monster baseline and other monster systems
- final outcome normalization on top

Do not resurrect old legacy power-side heuristics beside this cleaned pipeline.


Current Active Calibration Workflow

The live balance calibration workflow is:

1. update active admin tuning where needed
2. export active tuning snapshots
3. run active harness mode
4. review pass/warn/fail
5. classify mismatches before tuning
6. tune only the correct owner layer
7. export snapshots again
8. rerun active harness

The required commands are:

npm run calibration:export-active-tuning

npx --yes tsx scripts/outcomeCalibrationHarness.ts --tuning active

Active harness mode is live balance truth.

Defaults mode is smoke-test fallback only.

Do not judge live balance from defaults mode.

The snapshot files are:

scripts/fixtures/tuning/active-power-tuning.json

scripts/fixtures/tuning/active-combat-tuning.json

scripts/fixtures/tuning/active-outcome-normalization.json

The active harness currently targets:

29 fixtures
29 pass
0 warn
0 fail
0 mismatches

If active harness is not clean, do not tune manually from vibes. Inspect the mismatch owner first.


Current Non-Power Baseline Truths Future Chats Must Preserve

- natural attacks and equipped weapons with the same authored at-will attack profile should produce the same baseline threat contribution
- natural protection and equipped protection with the same authored defensive profile should produce the same baseline survivability contribution
- dodge is physical-only in monster survivability baseline work
- Dodge succeeds on match or exceed
- mental protection has its own calculator-economy seam and should not be forced back into the exact same direct-budget curve as physical protection
- the asymmetry target is:
  - physical is broader and safer at baseline
  - mental is narrower, lacks dodge, and may therefore be cheaper or more efficient to buy in the relevant economy layer
- do not invent mental dodge merely to fake symmetry
- resist dice are defensive capability and do not emit offensive radar axes by themselves
- health pool parity is baseline, not automatic survivability reward
- PPV and MPV stress fixtures are part of the current calibration shield


Rule for Future Packet Resolution Work

All future packet-resolution work should assume:

- Packet 1 is always Primary
- only Packet 1 is defended against
- Secondary Packets remain contingent
- Packet 1 lands if it applies at least one operative result unit
- wound-based Secondary scaling uses Applied Success Bands = CEILING(Net Wounds / Wounds Per Success)
- mixed-intention legality is constrained by hostile-primary ordering
- packet timing legality must not smuggle in a different chassis
- Attached powers distinguish initial legal range from later host-bound persistence
- Channelled Attached reinforcement requires the target to remain within legal cast range from the source
- hostile delayed-entry Attached structures must keep gate on attach and gate on payload explicit


Rule for Future Runtime Work

Runtime and tracking tools are later work.

Do not let future chats accidentally design a second contradictory state model.

Printable effect slips and digital effect tracking should grow from the same power structure as the builder outputs.

Do not let runtime speculation contaminate builder truth before the builder is stable.


Rule for Future Real-Monster Validation Work

Use the Real Monster Outcome Validation Checklist when field-testing authored monsters.

Before judging a monster:

- export active tuning snapshots
- run active harness mode
- confirm FULL ACTIVE SNAPSHOT
- confirm active harness pass/warn/fail counts
- write expected radar bands before recalculating the monster
- compare actual radar to expected archetype shape
- inspect raw values and main contributors before assigning blame

Do not tune from one manual monster unless it exposes a clear routing/code bug.

If a mismatch seems real, add it as a future calibration fixture first, then tune against the fixture set.

Existing monsters authored before Pressure / Presence became visible may need re-authoring rather than global tuning.


Rule for Future Implementation Chats

Implementation chats should work from current files only.

Do not reason from memory if a newer file version exists.

Do not produce schema or UI assumptions against unseen code.

If current files are missing, request them.

If a remaining unresolved design boundary does not materially block implementation, do not stop to philosophize about it.

If a remaining unresolved design boundary does materially block implementation, resolve only the smallest hard rule needed to unblock honest execution.

If the user asks for repo review, treat the pushed GitHub commit or branch they name as the canonical review truth.

If the user asks for local-worktree review instead, require the current file or diff rather than pretending GitHub matches local state.


Current Open Questions That Are Real

The following questions are still genuinely open and should be treated honestly:

- the final schema ownership boundary for Lifespan
- the final schema boundary between Power-Level Fields, chassis-local fields, packet-local fields, and derived metadata
- the exact long-term treatment of costs that are currently cost-only versus later translated through cadence, realization, or structural pressure
- the exact monster-side realization modifier layer that should gate typed contributor output through monster attributes without contaminating canonical power truth
- the correct long-term low-end and breakpoint valuation for physical-only dodge under match-or-exceed rules
- the correct long-term economic relationship between PPV and MPV in Forge and in monster baseline contribution
- whether low-end mental survivability visibility needs Outcome Normalization help after raw package tuning is considered stable
- whether active real-monster validation reveals new archetypes missing from the current 29-fixture harness

These are real open questions.

They are not permission to pretend the whole architecture is still vague.


Rule for Future Save-Back Work

When a future chat changes something meaningful, update the right source file instead of trusting chat memory.

Use this update pattern:

- architecture and authoritative rules language go in 02_Power_System_Spec
- project progress and priority changes go in 03_Current_Phase_And_Next_Steps
- reusable continuity guidance and chat-openers go in 04_Implementation_Handoffs
- Phase 6 tooling / output / formula-boundary updates go in 05_Phase_6_Cost_Integration_Blueprint
- implementation-sequence updates go in Power implementation steps

Do not dump temporary argument sludge into the source docs.

Save back only durable truth, durable guidance, or a real implementation-facing blueprint.


Current-State Implementation Opener

Use the following opener when starting the next chat for continued implementation work:

Use the project sources 00_Master_Implementation_Plan, 01_Locked_Decisions_Log, 02_Power_System_Spec, 03_Current_Phase_And_Next_Steps, 04_Implementation_Handoffs, 05_Phase_6_Cost_Integration_Blueprint, and Power implementation steps as source of truth.

The shared power architecture is implemented behind Summoning Circle in first-pass form.

Phases 1–5 are complete enough to close as design phases.

Phases 7 and 8 are substantially complete.

Phases 9 and 11 are late-stage builder-surface cleanup passes and are close enough to close for the current Summoning Circle surface, even though niche edge cases may still appear.

Phase 6 is active implementation and already includes:

- canonical power-cost resolver
- versioned Power Tuning
- versioned Outcome Normalization
- versioned Combat Tuning / Monster Baseline Tuning
- first-pass derived cooldown resolver output; Cooldown Reduction remains future work
- active tuning snapshot export workflow
- Outcome Calibration Harness with active/default modes
- active snapshots as live balance truth
- source defaults as smoke-test fallback only
- 29 active calibration fixtures currently expected to pass with 0 warnings
- cleaned radar pipeline where canonical power contribution and non-power contribution feed outcome normalization

Before changing files:

1. Run git status.
2. Run npm run calibration:export-active-tuning.
3. Run npx --yes tsx scripts/outcomeCalibrationHarness.ts --tuning active.
4. Confirm active harness status.
5. If active harness is not clean, resolve that before unrelated tuning.

The following are treated as locked for this session:

- a power consists of Power-Level Fields, one Descriptor Chassis, and one or more Effect Packets
- Shared Rules Grammar remains separate from Delivery Method
- Delivery Method is implemented through Descriptor Chassis
- the hardened chassis matrix is Immediate, Field, Attached, Trigger, and Reserve
- Packet 1 is always the Primary Packet
- only Packet 1 sets the Defence Gate
- Secondary Packets never create separate defended entry points and only resolve if the Primary Packet lands
- Packet 1 lands if it applies at least one operative result unit
- wound-based Secondary scaling uses Applied Success Bands = CEILING(Net Wounds / Wounds Per Success)
- mixed-intention legality is constrained by hostile-primary ordering
- Lifespan, Effect Timing, and Effect Duration are separate concepts
- Counter is an authored Power-Level Field with No / Yes
- Assist is universal by system rule
- Responses are the player-facing resource
- Commitment Modifier is a Power-Level Field with Standard / Channel / Charge
- Channel is an action-commitment modifier only in first implementation; authored packet timing determines when effects occur
- first-pass Channel support is limited to Immediate, Field, and Attached
- Charge uses the player-facing modes Delayed Cast and Build Power
- Delayed Cast is not Reserve-only
- Build Power uses bonus dice per charged turn
- Delayed Cast cannot be Counter-enabled
- secondary scaling derives from the Primary result class in the current builder surface
- persistent states use stack cleanup through resist rolls, cleanse, natural degradation, and expiry
- ongoing damage uses ongoing value plus duration rather than stacks
- packet-local recipient class may distinguish Primary Targets, Allies, and Self where needed
- Allies includes self by default unless an explicit authored restriction says otherwise
- Trigger powers may use Trigger Method to distinguish Arm and then target from Target and then arm while keeping Packet 1 on trigger
- Secondary timing choices must obey both chassis legality and carrier legality
- Immediate remains the only chassis that can honestly have no Lifespan requirement in the current builder surface
- Field, Attached, Trigger, and Reserve require Turns or Passive and default to Turns
- overlapping areas from the same packet at the same timing use one power roll, pre-defence multiplication by overlap count, and one defence roll
- Dodge succeeds on match or exceed
- natural and equipped at-will attack anchors are normalized to the same baseline law
- natural and equipped protection anchors are normalized to the same baseline law
- dodge is physical-only in monster survivability baseline work
- mental protection has its own calculator-economy seam and may be tuned independently from physical protection
- resist dice do not create offensive radar axes
- simple powers must stay simple
- complexity increases cost
- live preview is mandatory

The current tuning-layer split is locked for this session:

- Power Tuning = canonical power truth
- Combat Tuning / Monster Baseline Tuning = non-power monster baseline contribution
- Outcome Normalization = downstream radar interpretation/display

Scope for this session:

- preserve the existing shared power architecture
- preserve the distinction between authored data, derived resolver logic, and preview wording
- preserve the three-layer tuning split
- tune only against active snapshot truth
- use real-monster validation to identify next fixture candidates
- resolve only genuinely blocking remaining builder, descriptor, or translation edge cases

Non-goals:

- do not reopen Phase 1–4 architecture unless a direct contradiction appears
- do not redesign the whole UI visually
- do not drift into runtime tracking systems
- do not solve full Transformation or Summoning payload builders
- do not invent mental dodge
- do not collapse the PPV/MPV asymmetry merely to make the spreadsheet look symmetric
- do not judge live balance from source-default mode
