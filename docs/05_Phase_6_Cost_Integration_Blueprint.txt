05_Phase_6_Cost_Integration_Blueprint.txt

Purpose

This document records the implementation-facing blueprint for Phase 6 cost integration.

It is not a design-theory document.

It exists to define:

- what Phase 6 is trying to accomplish
- how the cost model should be structured
- which parts now already exist in code
- which parts are still unfinished
- which boundaries future chats must preserve when continuing the work


Phase 6 Goal

Phase 6 exists to price the hardened shared power system honestly.

That means the cost model must reflect:

- shared cast context burden
- structural chassis burden
- access / action-commitment burden
- packet identity
- packet magnitude
- packet timing
- packet duration
- packet recipient
- specific mechanic burden
- packet count complexity
- cross-packet leverage

The cost model must support both:
- future Character Builder scalar spend
- Summoning Circle monster-side derived axis contribution

There must still be one underlying cost truth.


What Phase 6 Already Has

The current implementation already includes:

1. Canonical power-cost resolution
- a live Phase 6 power-cost resolver exists
- packet-level and total power breakdowns are inspectable
- canonical power contribution now feeds the monster-side calculator through a cleaned integration path

2. Versioned Power Tuning
- active / draft / archived sets
- grouped admin UI
- friendly labels / descriptions
- searchable current keys
- comparison and reset workflow

3. Versioned Outcome Normalization
- active / draft / archived sets
- grouped admin UI
- current radar-curve / display tuning exposed separately from power truth

4. Versioned Combat Tuning / Monster Baseline Tuning
- active / draft / archived sets
- grouped admin UI
- non-power monster baseline contributors exposed separately from power truth and radar normalization

5. Canary Harness
- shared approved canary catalog
- shared approved scenario catalog
- baseline-versus-candidate comparison for:
  - Power Tuning
  - Outcome Normalization
  - Combat Tuning
- shared stripped Summoning Circle baseline so live editor and harness now align

6. Codex-run Combat Lab simulation workflow
- Codex-run Combat Lab simulation batches are accepted as an incremental balance workflow when provenance is reported
- manual Combat Lab runs remain required for major milestone confidence, UI/report readability checks, and table-feel judgement
- Codex-run simulations should be used for repeated scenario matrices, variant comparison, canary checks, outlier detection, and checking whether a patch moved win rates or diagnostics
- simulation reports must distinguish whether results came from UI/browser inspection, API routes, scripts, direct runtime calls, or another method
- Codex must not silently change tuning, source data, saved monsters, database data, or code during read-only simulation passes

7. Cleaned radar pipeline
- canonical power contribution is derived first
- non-power monster contribution is added second
- outcome normalization is applied last
- legacy independent power heuristics have been removed or bypassed


What Phase 6 Must Still Preserve

Even though major tooling now exists, Phase 6 must still preserve the following hard rules:

- formula shape lives in code
- tunable numeric values live in admin-editable config
- one canonical underlying power-cost truth
- no duplicate independent balance systems
- cooldown is derived after BasePowerValue
- Cooldown Reduction is not part of the base formula and remains future work
- simple powers must stay simple
- not every cost node automatically becomes a visible radar lane


Canonical Output Split

Character Builder side:
- shows a total Power Point pool
- consumes a scalar spend value per power
- supports players allocating that pool across few or many powers

Summoning Circle side:
- consumes a derived axis contribution vector from powers
- combines that vector with non-power monster contribution
- then applies outcome normalization for radar display

This means:
- one shared costing truth
- multiple downstream consumption layers
- no duplicate independent tuning system


Current Phase 6 Layer Split

The current implementation now has three distinct tuning layers.

Power Tuning
- canonical power truth

Combat Tuning / Monster Baseline Tuning
- non-power monster baseline contribution

Outcome Normalization
- downstream radar interpretation and display

Future chats must not collapse these into one blurred tuning page or one blurred mental model.


Cooldown Rule For Phase 6

Cooldown is not a cost input.

Cooldown is a derived output from the base power value.

The intended cost sequence remains:
- calculate BasePowerValue
- derive cooldown band / cooldown value from BasePowerValue
- optionally allow additional spend to reduce that cooldown where legal

Cooldown Reduction is a separate paid modifier.

Cooldown Reduction is not part of the base authored power value and should be layered on top as additional spend later.

Cooldown value and cooldown lifecycle are separate concepts.

Cooldown value answers:
- what number is placed on the cooldown die once cooldown begins

Cooldown lifecycle answers:
- when the cooldown die is placed
- when it is reduced
- when the power becomes ready again

Cooldown does not begin merely because a power is cast, armed, attached, placed, held, or released.

Cooldown begins only when the power has completely resolved.

Immediate chassis powers completely resolve once application successes have been rolled, required defence / protection / dodge / resist resolution has occurred, results have been compared, unblocked results have been applied, and any continuing duration tracking has been noted separately.

Non-Immediate powers with Lifespan Turns X treat the cast / execution / release turn as Turn 0 and remain active through X subsequent caster-turn rounds.

Those powers enter cooldown only after the final resolution window of the last lifespan turn has closed.

The final resolution window closes once the last legal target has finished the relevant turn, trigger, entry, expiry, or interaction that could be affected by the power during that final lifespan turn.

Passive Lifespan powers do not enter cooldown because time passes. They enter cooldown only when voluntarily ended by the caster as a Response or Power Action, or when Cleanse / Resist reduces currently active successes to 0.

Counters and Assists use the same lifecycle and treat the executing Response as Turn 0.

Charge is front-loaded. Charge turns occur before release or cast. Lifespan tracking begins only when the charged power is released, cast, or otherwise executed.

Once a cooldown die is placed, reduce it at the end of each subsequent turn the owning character takes. If the die is showing 1 when it would be reduced, remove it instead. Once removed, the power is ready on the owner's next turn or legal Response window, subject to the power's normal permissions.

Combat Lab, runtime, and encounter tooling must consume the derived cooldown value but must not treat cooldown as starting on cast unless the power also fully resolves on cast.


Canonical Cost Formula Shape

The base formula shape for Phase 6 is:

BasePowerValue =
  SharedContextFormula
  + StructuralFormula
  + AccessFormula
  + CombinedEffectPacketFormula

Cooldown is derived outside the base formula.

If cooldown reduction is purchased in later work, then:

FinalPowerSpendValue = BasePowerValue + CooldownReductionFormula


SharedContextFormula

SharedContextFormula prices the shared cast context that applies across the whole power.

It contains:
- Range category
- target count choices
- ranged distance
- AoE cast range
- AoE count
- AoE shape
- AoE dimensions

SharedContextFormula should be additive.

It is a real cost burden, but it should not be allowed to dominate packet identity.

Targeting flexibility cost guidance

Self-only targeting is the cheapest and cleanest targeting profile.

A power that can affect self and another ally / target is more flexible than a self-only power and should pay for that flexibility through target legality / recipient flexibility where the current architecture supports it.

A power that can choose multiple targets is more flexible than a single-target power.

Target count remains meaningful because self consumes one selected target slot unless a power explicitly says otherwise.

Broader range, area, or flexible target profiles should be valued by the cost model rather than blocked by an extra authoring rule that Melee or Ranged powers cannot target self.


StructuralFormula

StructuralFormula prices structural burden created by the chassis and its presence pattern.

It contains:
- chassis base cost
- structural presence / lifespan burden
- trigger / release structure burden where relevant
- delayed-payload / carrier burden where relevant

StructuralFormula should be mostly additive.

This is where premium non-immediate leverage must be priced honestly.


AccessFormula

AccessFormula prices use access and action-commitment burden.

It contains:
- Counter access burden
- Commitment Modifier burden
- charge-turn burden where relevant

AccessFormula should be mostly additive.

Build Power bonus output may also influence packet magnitude through a local packet-level scaling step rather than only this top-level access formula.


Counter And Response Cost Guidance

Counter: Yes is an added use permission, not an exclusivity tradeoff.

Do not discount a power because Counter-enabled use replaces normal active defence; the power remains normally usable as a Power Action.

Attack Counters carry a high premium because they create off-turn damage or effects and the triggering attacker does not receive an active defence roll against them. Already-active passive/static protection still applies.

Defence Counters are priced as specialist active-response alternatives, not as free stacks on top of Dodge, Physical Defence, Mental Defence, or Resist.

Movement and Control interrupt Counters need a high premium because a strict timing win can avoid, cancel, or deny an incoming action. First-pass timing requires strictly more successes than the trigger; ties fail.

Persistent or passive setup remains valuable because it is established before the response window and can stack with later response choices.

Preparation stacks. Panic buttons replace.

Counter, Movement Counter, and Control interrupt Counter costs should be revisited after Combat Lab and runtime behaviour faithfully implement the response-stack rules.


Duration Defence Pool Cost Guidance

Duration Dodge, Physical Block, Mental Block, and Resist powers create finite pools.

They must not be valued as if they rerolled or refreshed their full result on every incoming attack. They also must not be valued as a single instant defence if their duration allows selective spending across several triggers.

The cost model should account for:

- dice increasing pool depth through successful power-roll successes
- potency increasing both total pool size and the standard per-trigger commitment cap
- duration increasing the opportunity to spend the pool at the best moments
- target count and recipient flexibility increasing how many actors can carry pools
- Dodge Pool value being high because it can help match-or-exceed physical attacks either as pool-only protection or as a supplement before an optional normal Dodge roll
- Resist Pool value being high because it can cancel hostile successes, stacks, ongoing units, or equivalent units in an authored resisted Attribute lane
- Block Pool value being more linear because it adds direct wound prevention in the matching physical or mental lane
- defensive-pool pricing must not assume cross-lane degradation; Physical Defence, Mental Defence, Dodge, and active Resist degradation are independent defence-lane tracks
- defensive-pool pricing must not assume pool spending always causes normal defence degradation; pool-only commitment spends finite points without rolling or degrading Dodge, Physical Defence, Mental Defence, or active Resist

For one applied duration defence:

Pool size = successful power-roll successes x potency.

Standard per-trigger cap = potency.

Costing should treat high dice + high potency + duration as a spike-and-pressure risk because it creates both a deeper pool and stronger individual trigger commits.

The calculator and Power Tuning should not value duration Dodge or duration Resist like ordinary attack potency, because those pools can avoid or cancel hostile packets rather than merely adding linear output.

Counter Dodge remains a one-shot response unless the power also explicitly creates a duration pool. Do not price a simple Counter Dodge as a persistent Dodge Pool, and do not price a persistent Dodge Pool as if it were only a one-shot Counter.

Chassis-specific defensive pool pricing must distinguish:

- one-time duration pool
- repeated refresh/replace pool
- Field repeated multi-target refresh
- Attached repeated target-following refresh
- Channelled repeated refresh with action economy cost
- Charged release with bonus dice
- Charged release with bonus potency
- explicit additive/recharging pool

Cost hierarchy guidance:

- Immediate one-time duration pool = baseline
- Trigger/Reserve delivery = normal pool rules after delivery, plus their own access/setup costs
- Attached refresh/replace pool = more expensive than one-time duration
- Field refresh/replace pool = more expensive when it affects multiple targets or an area
- Channelled refresh/replace pool = discounted for action economy sacrifice but still more expensive than one-time duration
- Charged dice/potency modifies the release pool and must be priced according to final release values
- Additive/recharging pools = very expensive special mechanic

Pricing matrix future work should account for:

- pool type: Dodge / Physical Block / Mental Block / Resist
- expected pool size
- per-trigger cap
- duration window
- expected reapplication count
- expected target count
- delivery chassis
- effect timing cadence
- target scope
- field-bound versus attached/follows-target behaviour
- channel maintenance cost
- charge dice versus charge potency
- additive/recharging special mechanics

Refresh/replace is the default repeated-application model. Additive or recharging defensive pools are not default behaviour and require explicit authoring, clear limits, and a heavy Power Tuning surcharge.


Defensive Pool Pricing Matrix

This section defines formula architecture and pricing inputs only. It does not set final numeric values.

Defensive pool pricing must not reuse ordinary Attack potency valuation.

The core problem is that defensive pools create finite defensive resources with two separate value shapes:

- total resource depth
- per-trigger burst capacity

Attack potency usually scales wounds or direct output. Defensive pool potency does more than that.

For Dodge Pools, potency scales both total pool size and the amount of Dodge threshold pressure that can be applied to one incoming physical / dodgeable packet.

For Resist Pools, potency scales both total pool size and the amount of hostile success, stack, ongoing-unit, or equivalent-unit cancellation that can be applied to one incoming hostile packet in the authored resisted Attribute lane.

For Block Pools, potency scales finite wound prevention and the per-trigger prevention cap.

Duration and reapplication create opportunity value: the pool can be saved for meaningful triggers rather than spent immediately.

Target scope creates multiplication value: more protected actors and more flexible recipient choices create more chances that the pool matters.

Pool-only use adds option value: a protected actor can spend finite pool points without also consuming a normal active defence degradation step. Pool plus normal defence still has supplement value when the defender chooses to roll the matching active lane.

Therefore defensive pool pricing needs separate depth, burst, duration, delivery, target, reapplication, pool-only option value, and exception factors.


Pool Depth Value Versus Per-Trigger Burst Value

PoolDepthValue answers: how long can this resource last?

It is based on expected total available pool points:

PoolDepthValue starts from:

expectedPowerSuccesses x potency

Dice count primarily improves expected pool depth, reliability, and the chance that a pool exists at a useful size.

PerTriggerBurstValue answers: how much can this pool change one incoming attack or effect?

It is based on:

potency / per-trigger commit cap

Potency improves both total pool depth and the per-trigger cap. This makes potency more dangerous than dice when pricing defensive pools.

High dice / low potency and low dice / high potency are not equivalent even if their expected total pool size is similar.

High dice / low potency usually creates reliable shallow coverage.

Low dice / high potency creates swingier but much more dangerous burst denial when it lands.

Charged potency is especially dangerous because it increases both pool size and per-trigger cap at release. Charged dice mainly increases reliability and expected depth.

BurstCapValue must be nonlinear for Dodge and Resist because threshold denial and cancellation can erase an entire hostile packet or key hostile unit package.


Pool Type Hierarchy

Physical Block Pool / Mental Block Pool

- linear wound prevention
- points add direct wound prevention, not defence successes
- lower burst danger than Dodge or Resist because they reduce wounds rather than canceling the packet
- still valuable because points can be saved for meaningful hits
- physical and mental lanes may have different tuning weights if PPV / MPV economics diverge
- Physical Block Pool and Mental Block Pool should remain separate tuning lanes if Combat Tuning keeps physical and mental survivability asymmetrical

Dodge Pool

- threshold-based
- points add Dodge successes
- can avoid a whole physical or explicitly dodgeable packet on match-or-exceed
- value spikes near common incoming success bands
- interacts with normal Dodge degradation only when normal Dodge is also rolled; pool-only Dodge Pool commitment does not degrade the Dodge lane
- high potency must be priced as burst packet-denial capacity, not merely extra mitigation
- Dodge Pool does not make mental attacks dodgeable unless an action explicitly allows Dodge

Resist Pool

- lane-limited cancellation
- points cancel hostile successes, stacks, ongoing units, or equivalent units in the authored resisted Attribute lane
- strong against debuffs, control, ongoing pressure, and other high-impact hostile units
- wrong lane does nothing
- one Defence -> Resist packet maps to one resisted Attribute
- multi-attribute Resist requires multiple packets and should pay packet/multi-lane costs
- generic or omni Resist, if ever authored, requires a major surcharge because it bypasses the lane-risk constraint


Delivery / Chassis Matrix

Immediate one-time duration pool

- baseline finite pool model
- creates the pool once on application
- prices depth + burst + duration opportunity
- does not imply repeated full-value rerolls

Trigger

- follows normal finite pool rules after delivery
- add trigger setup/access cost
- value depends on trigger reliability and whether the trigger fires at a tactically ideal moment
- if trigger timing gives strong tactical selection, price the timing advantage

Reserve

- follows normal finite pool rules after release
- add hold/release flexibility cost
- value depends on how much tactical timing control the user has
- release flexibility can make the same pool more valuable than an immediate pool

Field

- creates target-specific pools
- location-bound by default
- cost increases with area size, expected legal targets, and delivery cadence
- repeated refresh/replace increases reliability and availability but is not additive stacking
- multi-target Field refresh is a major multiplier
- field-bound limitation should reduce value compared with attached/following protection when targets can leave the area
- lingering Field pools require explicit authoring and a surcharge

Attached

- follows the attached target while the attachment persists
- more reliable for that target than location-bound Field
- repeated refresh/replace increases availability
- target-following protection costs more than equivalent location-bound protection
- if the pool lingers after attachment ends, price that as a special exception

Channel

- refresh/replace while maintained
- no compounding by default
- receives action-economy credit/discount because the user pays maintenance cost
- still more expensive than one-time duration because it can repeatedly refresh protection
- channelled area or multi-target protection remains expensive despite action cost
- Channel does not automatically repeat packet resolution; authored Effect Timing controls cadence

Charge

- priced at final release values
- bonus dice increase reliability and expected pool depth
- bonus potency increases pool depth and per-trigger cap
- charged potency on Dodge or Resist needs heavy burst-denial pricing
- charge action cost offsets some value but does not erase final-output danger
- charge turns before release do not count against pool duration

Additive / recharging pools

- not default behaviour
- special mechanic only
- requires explicit authoring, maximum pool cap, clear expiry, explicit stack/recharge wording, and heavy surcharge
- should have separate pricing from refresh/replace
- should not be inferred from Field, Attached, Channel, Trigger, Reserve, or Charge


Duration And Cadence Model

Duration value depends on opportunity, not just number of turns.

DurationOpportunityFactor should consider:

- duration window
- expected incoming trigger count
- probability the protected target is attacked
- target priority likelihood
- start/end turn cadence
- reapplication count
- whether reapplication is refresh/replace or additive
- whether the pool is field-bound, attached, channelled, or charged
- whether the user can choose optimal spend moments

Refresh/replace must not be priced as full additive pool generation.

Refresh/replace increases reliability, uptime, and availability. It does not create stacked total capacity unless the power explicitly uses additive/recharging authoring.

Repeated cadence should therefore price expected improvement in usable remaining pool, not sum every application as new independent pool value.


Targeting / Recipient Matrix

TargetScopeFactor should scale with both:

- number of protected actors
- flexibility of who can receive protection

Targeting factors should distinguish:

- self only
- one ally
- self or ally
- multiple chosen targets
- all allies in area
- all legal targets in Field
- attached to one target
- attached to object / host / area
- field-bound versus follows target
- can affect self plus others

Self-only is the baseline cheapest recipient profile.

One chosen ally is narrower than self-or-ally if the caster cannot protect themself.

Self or ally is more flexible than either self-only or ally-only.

Multiple chosen targets multiply expected protected actors and should scale with target count.

All allies in area and all legal targets in Field require both target-count and area/reach pricing.

Target count and self-slot rules are locked elsewhere: self consumes a selected target slot unless a power explicitly says otherwise.


Formula Skeleton

Non-final formula shape:

DefensivePoolPacketValue =
  PoolTypeBase
  x PoolDepthValue
  x BurstCapValue
  x DurationOpportunityFactor
  x DeliveryReliabilityFactor
  x TargetScopeFactor
  x ReapplicationFactor
  x SpecialMechanicFactor
  + ComplexityPremiums

PoolTypeBase:

- baseline value for Block, Dodge, or Resist
- may distinguish Physical Block and Mental Block if PPV / MPV economics diverge

PoolDepthValue:

- expectedPowerSuccesses x potency
- includes dice reliability and expected generated points
- mostly values how long the resource can last

BurstCapValue:

- potency / per-trigger cap
- nonlinear for Dodge and Resist
- more linear for Block
- must account for cap overrides

DurationOpportunityFactor:

- estimates how many meaningful spend opportunities are expected
- considers duration, target priority, incoming trigger count, and cadence

DeliveryReliabilityFactor:

- distinguishes Immediate, Trigger, Reserve, Field, Attached, Channel, and Charge
- accounts for timing control, positional limits, maintenance cost, and final release values

TargetScopeFactor:

- prices who and how many actors can receive pools
- accounts for self-only, ally-only, self-or-ally, multi-target, area, and attachment modes

ReapplicationFactor:

- prices refresh/replace cadence and reliability
- must not treat refresh/replace as additive pool generation
- additive only applies where explicitly authored

SpecialMechanicFactor:

- additive/recharging pools
- generic Resist
- multi-attribute Resist
- combined-pool spending
- lingering Field pools
- cap overrides

ComplexityPremiums:

- additional cost for rule burden, multi-packet structure, exceptions, and exploit surface
- should be cost-only unless the mechanic genuinely creates battlefield axis expression

Actual numeric values belong in Power Tuning.

Outcome Normalization must not hide bad defensive pool pricing.

Combat Tuning must not be used to patch power-cost errors.


Future Power Tuning Key-Family Plan

The following are planning names only. Do not treat them as existing config keys.

Likely future key families:

packet.defensivePool.type.block.base
packet.defensivePool.type.dodge.base
packet.defensivePool.type.resist.base

packet.defensivePool.depth.expectedWeight
packet.defensivePool.depth.diceReliabilityWeight

packet.defensivePool.burst.potencyCapWeight
packet.defensivePool.burst.dodgeThresholdMultiplier
packet.defensivePool.burst.resistCancellationMultiplier
packet.defensivePool.burst.blockLinearMultiplier

packet.defensivePool.duration.turns.1
packet.defensivePool.duration.turns.2
packet.defensivePool.duration.turns.3
packet.defensivePool.duration.passive

packet.defensivePool.reapplication.none
packet.defensivePool.reapplication.refreshReplaceMultiplier
packet.defensivePool.reapplication.additiveSurcharge

packet.defensivePool.target.self
packet.defensivePool.target.singleAlly
packet.defensivePool.target.selfOrAlly
packet.defensivePool.target.multiTarget
packet.defensivePool.target.fieldArea
packet.defensivePool.target.allAlliesInArea

packet.defensivePool.delivery.immediate
packet.defensivePool.delivery.trigger
packet.defensivePool.delivery.reserve
packet.defensivePool.delivery.field
packet.defensivePool.delivery.attached

packet.defensivePool.commitment.channelDiscount
packet.defensivePool.commitment.chargeDiceWeight
packet.defensivePool.commitment.chargePotencyWeight

packet.defensivePool.resist.genericSurcharge
packet.defensivePool.resist.multiAttributeSurcharge

packet.defensivePool.exception.combinedPoolSpendSurcharge
packet.defensivePool.exception.lingeringFieldSurcharge
packet.defensivePool.exception.capOverrideSurcharge

These names should be refined when the resolver seam is implemented. The important point is the axis of separation: pool type, depth, burst, duration, reapplication, target scope, delivery, commitment, and exceptions must be independently tunable enough to avoid one blunt defensive-pool multiplier.


Defensive Pool Canary And Scenario Requirements

Active numeric tuning must not begin until synthetic canaries exist that actually create and spend defensive pools.

The detailed canary source of truth is docs/14_Defensive_Pool_Canary_Matrix.txt.

That matrix defines:

- Layer A deterministic/assertion canaries A1-A12
- Layer B batch balance canaries B1-B8
- exact synthetic setup and pressure shape for each canary
- expected Defensive Pool Diagnostics
- pass/fail assertions
- unsupported/deferred chassis cases
- implementation notes for scripts/combatLab.scenarioMatrix.ts
- Codex-run provenance requirements

All future defensive-pool pricing evidence should come from named canary scenarios or Codex-run Combat Lab batches with provenance.

Manual milestone testing remains required for major rules changes, large balance shifts, playtest packets, public/demo builds, UI/report readability, and table-feel judgement.

Codex-run batches are preferred for incremental balance passes, variant comparison, canary checks, scenario matrices, outlier detection, and repeated evidence gathering.


CombinedEffectPacketFormula

CombinedEffectPacketFormula is the heart of the power cost model.

It contains:
- PrimaryPacketFormula
- SumOfSecondaryPacketFormulae
- PacketCountComplexityFormula
- CrossPacketSynergyFormula


PrimaryPacketFormula

The Primary Packet is the main defended entry point and should be priced as the full leading effect.

PrimaryPacketFormula contains:
- EffectFamilyBaseFormula
- MagnitudeFormula
- PacketTimingFormula
- PacketDurationFormula
- RecipientFormula
- SpecificMechanicFormula
- PersistenceCleanupFormula
- ScalingFormula where relevant

MagnitudeFormula uses explicit Power Tuning keys for each supported discrete authoring value. Dice count and potency are discrete magnitude controls, so every legal authored value must be represented by a key such as packet.magnitude.dice.N or packet.magnitude.potency.N. Authoring surfaces must not expose unpriced dice/potency values, because bucket fallback is intended for continuous/range-style tuning lookups, not for flattening legal packet magnitude.

Required packet fields are validity gates, not alternate pricing branches. For example, Attack packets without a selected damage type are incomplete and must not produce a valid monster threat/radar output or player spend comparison.

For a fixed legal power shape, increasing supported dice count or potency must not reduce BasePowerValue, the canonical power-axis contribution, or the final normalized radar output unless the output is already clamped at its documented cap. Player level-sensitive spend scaling remains deferred until canonical cost and radar monotonicity are stable.


SecondaryPacketFormula

Secondary Packets are real value but contingent.

Each SecondaryPacketFormula should therefore be calculated as its own packet value and then reduced by a contingency factor rather than treated as either free or fully independent.

For wound-band Secondary scaling, the default band size is the Primary Packet's
total simultaneous wounds per success across all damage components. Multiple
simultaneous damage types increase the denominator; they do not create separate
free Secondary rider engines. Component-specific or per-component additive
wound-band engines are special authored mechanics and require explicit pricing.

TODO / unresolved design debt: decide whether wound-band source should use
actual wounds suffered after remaining-health cap or net wounds before overkill
is discarded. Current recommendation is actual wounds suffered by default, with
overkill-scaling reserved for a future explicit premium mechanic if allowed.

Vulnerability, Resistance, and Protection modify the defence roll when their
triggering damage types are present. They do not modify wounds directly and do
not alter Primary wound-band denominators. Resolve the Primary Packet normally,
including V/R/P effects on the defence roll, then calculate wound bands from the
resulting Primary wound total using the Primary Packet's authored total
simultaneous wounds per success as the default band size.

There is no active general Immunity rule. Damage immunities are future Monster
Trait design debt and must define their own costing and interaction with
defence rolls, wound results, and Secondary wound-band scaling if implemented.

SecondaryPacketFormula_i =
  (PacketCoreFormula_i + PacketModifierFormulae_i)
  × SecondaryContingencyMultiplier_i


PacketCountComplexityFormula

This formula prices the burden of having multiple packets regardless of what those packets do.

It exists because:
- more packets increase exploit surface
- more packets increase complexity
- more packets increase leverage density

This is cost-only by default.

It should not emit axis by itself.


CrossPacketSynergyFormula

This formula prices additional leverage that emerges from packet combinations rather than from isolated packet values.

Examples include:
- hostile-primary into beneficial rider
- delayed-payload leverage
- recurring leverage created by a real carrier
- result-scaling follow-through
- other packet combinations that are stronger together than as isolated dropdown values

This is additive by default.

Axis emission from this node is conditional and must follow actual battlefield shape rather than defaulting blindly into Synergy.


Cost-Only / Modifier-Only / Axis-Carrying Split

The Phase 6 model now explicitly distinguishes three types of cost node:

Cost-Only
- adds scalar cost
- emits no axis directly

Modifier-Only
- modifies other value expressions
- may add scalar cost
- should not emit large raw axis value by itself

Axis-Carrying
- adds scalar cost
- may also emit derived axis contribution where honest

This matters because not every expensive thing should automatically become a visible radar spike.

Examples:
- Packet Set Complexity is cost-only
- Shared Cast Context is modifier-only
- Structural Presence may emit modest Pressure / Presence
- Cross-Packet leverage may emit axis only where the combination truly changes battlefield shape


Current Translation Order

The current intended order is:

1. derive canonical cost breakdown
2. derive canonical per-use power axis contribution
3. add non-power monster contribution
4. apply outcome normalization
5. display the final monster radar

Later work may still add:
- cadence / availability translation improvements
- monster-side realization modifier layer
- other missing downstream translation seams

But those belong after canonical power truth, not inside it.


Current Open Phase 6 Questions

The following are still real open questions inside Phase 6:

- how the defensive pool pricing matrix should value Dodge Pool, Physical Block Pool, Mental Block Pool, and Resist Pool across duration, potency, delivery chassis, refresh cadence, target count, and per-trigger cap
- implementing the defensive-pool canary fixtures and scenario matrices defined in docs/14_Defensive_Pool_Canary_Matrix.txt before numeric pricing changes
- which Power Tuning key seams are needed for defensive pool pricing without flattening every pool type into one generic modifier
- which current costs should remain cost-only versus later translated into cadence, structural pressure, or realization
- how the later monster-side realization modifier layer should gate typed contributor output through monster attributes
- which current downstream translation steps still under-report or over-report real battlefield strength
- whether additional debug or comparison surfaces are still needed for faster tuning iteration

These are now implementation-facing tuning questions, not permission to reopen the whole formula architecture.


Codex-Run Simulation Evidence Requirements

For Phase 6 tuning, Codex-run Combat Lab results are evidence only when the report includes:

- commit SHA
- git status / worktree cleanliness
- exact command, script, route, API endpoint, direct runtime call, or other method used
- whether browser UI was visually inspected
- run count
- seed or seed policy
- scenario names
- actor and monster names
- active Power Tuning name
- active Combat Tuning name
- active Outcome Normalization name
- any temporary in-memory overrides
- confirmation that no database, source-data, saved-monster, tuning, or code changes were made during read-only simulation
- validation or smoke status where relevant

Recommended report fields are player win %, monster win %, stalemate %, average rounds, player damage per round, monster damage per round, action use counts, cooldown-prevented counts, defensive choice diagnostics, counter candidate diagnostics, ongoing pressure diagnostics, defensive pool diagnostics, transcript anomalies, and unsupported runtime notes.

Codex-run simulation results are not a replacement for manual milestone testing. They are a repeatable evidence layer for narrowing candidate values before human feel checks.


Immediate Practical Phase 6 Direction

The immediate Phase 6 work should stay focused on:

- using docs/14_Defensive_Pool_Canary_Matrix.txt as the defensive-pool canary/scenario fixture source of truth
- implementing defensive-pool canaries in scripts/combatLab.scenarioMatrix.ts and/or smoke assertions
- running Codex defensive-pool canary batches with full provenance and inspecting their outputs
- defining Power Tuning key seams for defensive pool pricing
- running defensive-pool canaries again after Power Tuning key seams are added
- tuning initial numeric values only after the pricing seams and canaries exist
- running manual milestone verification for readability, UI, and table-feel checks after numeric changes
- returning to Dire Wolf / Sudden Leap / Swiping Claws tuning only after the defensive pool pricing path is stable
- tuning specific powers against the aligned live/harness baseline
- tuning non-power monster baseline contribution where stripped monsters are too weak or too strong
- tuning outcome normalization where radar display saturates too early or too late
- preserving clean separation between:
  - Power Tuning
  - Combat Tuning
  - Outcome Normalization
- beginning the monster-side realization modifier layer only when current tuning layers are stable enough to justify it
